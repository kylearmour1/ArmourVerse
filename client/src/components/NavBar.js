import React from 'react';

function NavBar() {
  return (
    <nav className="navbar">
      {/* Add links or icons for navigation */}
      <a href="/">Home</a>
      <a href="/profile">Profile</a>
      {/* Add more navigation items */}
    </nav>
  );
}

export default NavBar;
